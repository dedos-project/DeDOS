image: alpine

pages:
    script:
    - apk update && apk add doxygen
    - cd runtime && doxygen Doxyfile
    - mkdir public
    - mv runtime/html public
    artifacts:
        paths:
        - public
    only:
    - master
    - dev
